diffex:
  sample_description_file: config/sample_description.csv
  comparison_file: config/comparison.yaml
  
input_dir: ./inputs/00-multiplexed_reads

main_factors:  yes
phenotypes: pLabel
samples:
    sampleA: pval1
    sampleB: pval2

comparisons:
    pLabel:
        - pval1_v_pval2

alignment_output_dir: alignment_results

trimming_options:
    base_quality_5prime: 0
    base_quality_3prime: 0
    trim_length_5prime: 3
    trim_length_3prime: 10

entrez_gene_info: foo.txt
genome:
references:
transcriptome_index:
fold_change:
deseq2_adjustedPValue:

alignment_options:
    library_type: fr-unstranded
    transcriptome_only: True
    read_length: 50
