## Comparisons

The following comparisons were performed. [REPLACE: Anything to note?]

```{r include = FALSE}
    comp_table = do.call(rbind, lapply(diffex_info, as.data.frame.list, stringsAsFactors=FALSE))
    comparisons_kable = kable(comp_table, row.names = TRUE, format.args = list(big.mark = ',', scientific = FALSE)) %>%
        kable_styling(bootstrap_options = c('striped', 'condensed')) %>%
        scroll_box(width = '100%')
```

```{r echo = FALSE, results = 'asis', message = FALSE}
    cat(comparisons_kable)
```
